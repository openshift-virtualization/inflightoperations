apiVersion: ifo.kubevirt.io/v1alpha1
kind: OperationRuleSet
metadata:
  name: temp-rules
spec:
  target:
    group: apps
    version: v1
    resource: deployments
  rules:
    - operation: Rollout
      expression: "true"
